# --- Database (Neon Postgres) ---
# Neon connection string, e.g. postgresql://user:pass@ep-xxx.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=

# --- NextAuth ---
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=
# Set to the deployed URL in prod (or http://localhost:3000 for local dev)
NEXTAUTH_URL=http://localhost:3000

# --- Google Cloud (Service Account) ---
# Base64-encoded service account JSON key (single env var keeps Vercel secret management simple)
GOOGLE_SERVICE_ACCOUNT_BASE64=
# Fallback: individual fields (used only if BASE64 is empty)
GOOGLE_CLIENT_EMAIL=
GOOGLE_PRIVATE_KEY=
# The Workspace user/group email that Gmail send-as + calendar ACL delegate to
GOOGLE_DELEGATE_EMAIL=

# --- App settings (bootstrap defaults, admin-managed in-app afterwards) ---
# Initial admin password (bcrypt hashed in-app on first run; leave blank to disable)
ADMIN_INITIAL_PASSWORD=
